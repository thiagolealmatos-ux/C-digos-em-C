#include <stdio.h>

typedef struct {
    char nome[50];
    float preco;
    int quantidade;
    
} Produto;

int main()
{
   int n;
   
   int maior = 0;
    
    printf("Quantos Produtos:");
   scanf("%d", &n);
   
   Produto produto[n];
  float valor_em_estoque = 0;
  float total_geral = 0;
 char NomeMaiorvalor[50];
   
   for(int i = 0; i < n; i++){
       printf("Produto %d|", i + 1);
       printf("Nome do Produto: ");
       scanf("%s", produto[i].nome);
       printf("Qual e o preço desse produto: ");
       scanf("%f", &produto[i].preco);
       printf("Quantos desse produto: ");
       scanf("%d", &produto[i].quantidade);
       
     
   }
  
  printf("\n------------RESUMO DA SUA COMPRA------------\n");
   for(int i = 0; i < n; i++){
        printf("\nNome do Produto: %s ", produto[i].nome);
        printf("\nPreço do seu produto: %.2f", produto[i].preco);
        printf("\nQuantidade desse produto: %d", produto[i].quantidade);
       
      float valor_em_estoque = produto[i].preco * produto[i].quantidade;
      total_geral += valor_em_estoque;
      
      if(valor_em_estoque > produto[maior].preco * produto[maior].quantidade) {
           maior = i;
      }
      printf("\nValor em estoque: %.2f", valor_em_estoque);
      printf("\n------------------------------------------------\n");
    }
      


   

     printf("\nTotal da sua compra: %.2f", total_geral);
     printf("\n------------------------------------------------\n");
   printf("\nNome do produto mais caro: %s", produto[maior].nome);
  printf("\nValor em estoque: %.2f", produto[maior].preco * produto[maior].quantidade);
    
    return 0;
}
