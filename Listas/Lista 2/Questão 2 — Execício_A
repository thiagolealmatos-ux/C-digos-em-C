#include <stdio.h>

int fatorial(int n){

if(n == 0){
    return 1;
}

return n * fatorial(n - 1);
}

int main()
{
    int n;
    
    printf("Digite um Número: ");
    scanf("%d", &n);
    
    printf("Fatorial do Número digitado: %d", fatorial(n));

    return 0;
}

